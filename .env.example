# Required
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Models (tutor / practice / flashcards use DEFAULT; grader uses GRADER)
ANTHROPIC_MODEL_DEFAULT=claude-sonnet-4-6
ANTHROPIC_MODEL_GRADER=claude-opus-4-7

# Access control (optional — leave blank to disable)
# When set, clients must send matching X-Passcode header
APP_PASSCODE=

# Rate limiting (per client IP)
RATE_LIMIT_PER_MIN=30
RATE_LIMIT_PER_DAY=300
